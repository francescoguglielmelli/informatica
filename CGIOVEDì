<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
</head>
<body>
  <h1>Numeri Primi</h1>
  <h3>Calcolo del numero dei Primi compresi nei vari intervalli di 100 unità fino a 10.000</h3>
    <div id="output">
    </div>


    <script>
        let arrayNumeriInteri=[];
        let arrayNumeriPrimi=[];
        let nnprimi=0;
        CaricaArray();
        let outputDiv=document.getElementById("output");
        for(let numero=1;numero<=10000;numero++)
        {
          let divisoriNumero=Divisori(numero);
          let divisoriString= "Numeri primi tra " + (numero-100) + " e "+ numero +" : ";
         

           if (numero%100==0)
           {
             let interprimi= (arrayNumeriPrimi.length-nnprimi);
             nnprimi= arrayNumeriPrimi.length;
             outputDiv.innerHTML+=" <p> "+ divisoriString +interprimi+  "</p>"
           }

        }

        function CaricaArray()
        {for(let i =0;i<=10000;i++)
            {arrayNumeriInteri.push(i);
            }
            console.log(arrayNumeriInteri)
        }

     function Divisori(n){
// la funzione da i divisori di n cioè trova i possibili divisori
// es: n=50   divisori: 1,2,5,10,25
            //attenzione creo un ciclo fino a n/2 che controlla
            // se il resto della divisione tra n e i numero che
            //cresce da 2 a n/2 sia =0
        let divisori=[];    
        let massimodivisore=n/2;
        for(i=2;i<=massimodivisore;i++ )
        {
             if (n%i==0)
               { 
                 divisori.push(i)
               }
        }
        if (divisori.length==0)
        {
            divisori.push("Primo")
            arrayNumeriPrimi.push(n)
        }
       return divisori;
     }
    </script>

</body>
</html>
